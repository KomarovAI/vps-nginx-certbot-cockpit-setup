name: Infrastructure Deploy (Idempotent Fixed)

on:
  push:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      force_deploy:
        description: 'Force redeploy even if no changes detected'
        required: false
        default: 'false'
        type: boolean
      skip_terraform:
        description: 'Skip Terraform, deploy Marzban only'
        required: false
        default: 'false'
        type: boolean

env:
  TERRAFORM_VERSION: "1.8.5"

jobs:
  terraform-deploy:
    name: Infrastructure Deploy
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Terraform
        if: github.event.inputs.skip_terraform != 'true'
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}
          
      - name: Validate base secrets
        env:
          REQ_1: ${{ secrets.VPS_HOST }}
          REQ_2: ${{ secrets.VPS_USER }}
          REQ_3: ${{ secrets.SSH_PRIVATE_KEY }}
          REQ_4: ${{ secrets.DOMAIN_NAME }}
          REQ_5: ${{ secrets.ADMIN_EMAIL }}
        run: |
          set -e
          echo "✅ Checking required secrets..."
          missing=0
          for var in REQ_1 REQ_2 REQ_3 REQ_4 REQ_5; do
            if [ -z "${!var}" ]; then echo "❌ ERROR: Required secret missing: $var"; missing=1; fi
          done
          [ "$missing" -eq 0 ] || { echo "❌ Missing required secrets"; exit 1; }
          echo "✅ All required base secrets are present"

      - name: Validate Marzban secrets (soft)
        env:
          M_DOMAIN: ${{ secrets.MARZBAN_DOMAIN }}
          M_ADMIN_U: ${{ secrets.MARZBAN_ADMIN_USERNAME }}
          M_ADMIN_P: ${{ secrets.MARZBAN_ADMIN_PASSWORD }}
          M_SHORTS: ${{ secrets.XRAY_REALITY_SHORT_IDS }}
          M_SNI: ${{ secrets.XRAY_REALITY_SERVER_NAMES }}
          M_PRIVKEY: ${{ secrets.XRAY_REALITY_PRIVATE_KEY }}
        run: |
          echo "🔍 Checking Marzban-related secrets (soft)..."
          [ -z "$M_DOMAIN" ] && echo "⚠️  WARN: MARZBAN_DOMAIN not set - using main domain"
          [ -z "$M_ADMIN_U" ] && echo "⚠️  WARN: MARZBAN_ADMIN_USERNAME not set - using default"
          [ -z "$M_ADMIN_P" ] && echo "⚠️  WARN: MARZBAN_ADMIN_PASSWORD not set - will generate"
          [ -z "$M_SHORTS" ] && echo "⚠️  WARN: XRAY_REALITY_SHORT_IDS not set - using defaults"
          [ -z "$M_SNI" ] && echo "⚠️  WARN: XRAY_REALITY_SERVER_NAMES not set - using defaults"
          [ -z "$M_PRIVKEY" ] && echo "⚠️  WARN: XRAY_REALITY_PRIVATE_KEY not set - will generate"
          echo "⚡ Proceeding with defaults/autogeneration if any are missing"
          
      - name: Prepare terraform files directory
        if: github.event.inputs.skip_terraform != 'true'
        run: |
          mkdir -p terraform/files
          cp install.sh terraform/files/install.sh
          cp setup-memory.sh terraform/files/setup-memory.sh || true
          cp update.sh terraform/files/update.sh || true
          chmod +x terraform/files/*.sh || true
          
      - name: Terraform Init
        if: github.event.inputs.skip_terraform != 'true'
        working-directory: terraform
        run: terraform init
          
      - name: Terraform Format
        if: github.event.inputs.skip_terraform != 'true'
        working-directory: terraform
        run: terraform fmt -recursive

      - name: Terraform Validate
        if: github.event.inputs.skip_terraform != 'true'
        working-directory: terraform
        run: terraform validate
          
      - name: Terraform Plan
        if: github.event.inputs.skip_terraform != 'true'
        working-directory: terraform
        env:
          TF_VAR_vps_host: ${{ secrets.VPS_HOST }}
          TF_VAR_vps_user: ${{ secrets.VPS_USER }}
          TF_VAR_ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
          TF_VAR_domain_name: ${{ secrets.DOMAIN_NAME }}
          TF_VAR_admin_email: ${{ secrets.ADMIN_EMAIL }}
          TF_VAR_cockpit_password: ${{ secrets.COCKPIT_PASSWORD }}
        run: |
          terraform plan -detailed-exitcode -out=tfplan || {
            code=$?; if [ $code -eq 2 ]; then echo "plan_has_changes=true" >> $GITHUB_ENV; else exit $code; fi
          }
          
      - name: Terraform Apply
        if: github.event.inputs.skip_terraform != 'true' && github.ref == 'refs/heads/main' && (env.plan_has_changes == 'true' || github.event.inputs.force_deploy == 'true')
        working-directory: terraform
        env:
          TF_VAR_vps_host: ${{ secrets.VPS_HOST }}
          TF_VAR_vps_user: ${{ secrets.VPS_USER }}
          TF_VAR_ssh_private_key: ${{ secrets.SSH_PRIVATE_KEY }}
          TF_VAR_domain_name: ${{ secrets.DOMAIN_NAME }}
          TF_VAR_admin_email: ${{ secrets.ADMIN_EMAIL }}
          TF_VAR_cockpit_password: ${{ secrets.COCKPIT_PASSWORD }}
        run: |
          echo "🚀 Applying Terraform changes..."
          terraform apply tfplan
          echo "✅ Terraform apply completed"

      - name: Deploy Marzban VLESS REALITY (Idempotent FIXED)
        if: success()
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          DOMAIN_NAME: ${{ secrets.DOMAIN_NAME }}
          MARZBAN_DOMAIN: ${{ secrets.MARZBAN_DOMAIN }}
          MARZBAN_ADMIN_USERNAME: ${{ secrets.MARZBAN_ADMIN_USERNAME }}
          MARZBAN_ADMIN_PASSWORD: ${{ secrets.MARZBAN_ADMIN_PASSWORD }}
          XRAY_REALITY_PRIVATE_KEY: ${{ secrets.XRAY_REALITY_PRIVATE_KEY }}
          XRAY_REALITY_PUBLIC_KEY: ${{ secrets.XRAY_REALITY_PUBLIC_KEY }}
          XRAY_REALITY_SHORT_IDS: ${{ secrets.XRAY_REALITY_SHORT_IDS }}
          XRAY_REALITY_SERVER_NAMES: ${{ secrets.XRAY_REALITY_SERVER_NAMES }}
          XRAY_PORT: ${{ secrets.XRAY_PORT }}
          XRAY_GRPC_ENABLE: ${{ secrets.XRAY_GRPC_ENABLE }}
          XRAY_QUIC_ENABLE: ${{ secrets.XRAY_QUIC_ENABLE }}
        run: |
          set -euo pipefail
          echo "🔐 Setting up SSH key..."
          echo "$SSH_PRIVATE_KEY" > /tmp/deploy_key
          chmod 600 /tmp/deploy_key

          echo "📂 Syncing Marzban files to server..."
          rsync -az -e "ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key" marzban/ "$VPS_USER@$VPS_HOST:/opt/marzban/"

          echo "⚙️  Configuring and deploying Marzban..."
          ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key "$VPS_USER@$VPS_HOST" \
            "DOMAIN_NAME='$DOMAIN_NAME'" \
            "MARZBAN_DOMAIN='${MARZBAN_DOMAIN:-$DOMAIN_NAME}'" \
            "MARZBAN_ADMIN_USERNAME='${MARZBAN_ADMIN_USERNAME:-admin}'" \
            "MARZBAN_ADMIN_PASSWORD='${MARZBAN_ADMIN_PASSWORD:-}'" \
            "XRAY_REALITY_PRIVATE_KEY='${XRAY_REALITY_PRIVATE_KEY:-}'" \
            "XRAY_REALITY_SHORT_IDS='${XRAY_REALITY_SHORT_IDS:-abcdef0123456789,fedcba9876543210}'" \
            "XRAY_REALITY_SERVER_NAMES='${XRAY_REALITY_SERVER_NAMES:-discord.com,www.yahoo.com}'" \
            "XRAY_PORT='${XRAY_PORT:-2083}'" \
            "XRAY_GRPC_ENABLE='${XRAY_GRPC_ENABLE:-true}'" \
            "XRAY_QUIC_ENABLE='${XRAY_QUIC_ENABLE:-true}'" \
            bash -s << 'EOF'
            set -euo pipefail
            MARZBAN_DIR="/opt/marzban"
            cd "$MARZBAN_DIR"
            [ -f .env ] || cp .env.example .env
            set_kv(){ local k="$1" v="$2"; [ -z "$v" ] && return 0; if grep -q "^$k=" .env; then sed -i "s#^$k=.*#$k=$v#" .env; else echo "$k=$v" >> .env; fi }
            set_kv DOMAIN_NAME "${MARZBAN_DOMAIN:-$DOMAIN_NAME}"
            set_kv ADMIN_USERNAME "${MARZBAN_ADMIN_USERNAME:-admin}"
            GENERATED_PASSWORD="$(openssl rand -hex 16)"; set_kv ADMIN_PASSWORD "${MARZBAN_ADMIN_PASSWORD:-$GENERATED_PASSWORD}"
            set_kv XRAY_REALITY_PRIVATE_KEY "${XRAY_REALITY_PRIVATE_KEY:-}"
            set_kv XRAY_REALITY_SHORT_IDS "${XRAY_REALITY_SHORT_IDS:-abcdef0123456789,fedcba9876543210}"
            set_kv XRAY_REALITY_SERVER_NAMES "${XRAY_REALITY_SERVER_NAMES:-discord.com,www.yahoo.com}"
            set_kv XRAY_PORT "${XRAY_PORT:-2083}"
            set_kv XRAY_GRPC_ENABLE "${XRAY_GRPC_ENABLE:-true}"
            set_kv XRAY_QUIC_ENABLE "${XRAY_QUIC_ENABLE:-true}"
            set_kv XRAY_JSON "/var/lib/marzban/xray_config.json"
            CURRENT_KEY=$(grep '^XRAY_REALITY_PRIVATE_KEY=' .env | cut -d= -f2 || echo ""); if [ -z "$CURRENT_KEY" ]; then mkdir -p keys; KEYPAIR=$(docker run --rm teddysun/xray xray x25519 2>/dev/null || echo ""); if [ -n "$KEYPAIR" ]; then PRIV=$(echo "$KEYPAIR" | awk '/Private/{print $3}'); PUB=$(echo "$KEYPAIR" | awk '/Public/{print $3}'); [ -n "$PRIV" ] && set_kv XRAY_REALITY_PRIVATE_KEY "$PRIV"; echo "$PUB" > keys/public.key; fi; fi
            sed -i '/^version:/d' docker-compose.yml || true
            docker compose --env-file .env up -d
            sleep 5; docker compose ps
            if command -v ufw >/dev/null 2>&1; then ufw allow 2083/tcp || true; ufw allow 2083/udp || true; fi
          EOF

          echo "🌐 Configuring Nginx reverse proxy (idempotent)..."
          scp -o StrictHostKeyChecking=no -i /tmp/deploy_key scripts/ci-setup-nginx-marzban.sh "$VPS_USER@$VPS_HOST:/tmp/ci-setup-nginx-marzban.sh"
          ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key "$VPS_USER@$VPS_HOST" "sudo DOMAIN_NAME=$DOMAIN_NAME bash /tmp/ci-setup-nginx-marzban.sh"

      - name: Provision Xray VLESS REALITY Config
        if: success()
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          XRAY_REALITY_PRIVATE_KEY: ${{ secrets.XRAY_REALITY_PRIVATE_KEY }}
          XRAY_REALITY_SHORT_IDS: ${{ secrets.XRAY_REALITY_SHORT_IDS }}
          XRAY_REALITY_SERVER_NAMES: ${{ secrets.XRAY_REALITY_SERVER_NAMES }}
          XRAY_PORT: ${{ secrets.XRAY_PORT }}
        run: |
          set -euo pipefail
          echo "🔐 Setting up SSH key..."
          echo "$SSH_PRIVATE_KEY" > /tmp/deploy_key
          chmod 600 /tmp/deploy_key

          echo "📋 Syncing Xray config template and provisioning script..."
          scp -o StrictHostKeyChecking=no -i /tmp/deploy_key marzban/xray_config.json.tpl "$VPS_USER@$VPS_HOST:/tmp/xray_config.json.tpl"
          scp -o StrictHostKeyChecking=no -i /tmp/deploy_key scripts/ci-provision-xray-config.sh "$VPS_USER@$VPS_HOST:/tmp/ci-provision-xray-config.sh"
          
          echo "🚀 Provisioning Xray VLESS REALITY config from template..."
          ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key "$VPS_USER@$VPS_HOST" \
            "XRAY_REALITY_PRIVATE_KEY='$XRAY_REALITY_PRIVATE_KEY'" \
            "XRAY_REALITY_SHORT_IDS='$XRAY_REALITY_SHORT_IDS'" \
            "XRAY_REALITY_SERVER_NAMES='$XRAY_REALITY_SERVER_NAMES'" \
            "XRAY_PORT='${XRAY_PORT:-2083}'" \
            bash /tmp/ci-provision-xray-config.sh

          echo "🔄 Restarting Marzban to apply Xray config..."
          ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key "$VPS_USER@$VPS_HOST" \
            "cd /opt/marzban && docker compose restart marzban"
          
          echo "🔍 Verifying Xray config application..."
          ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key "$VPS_USER@$VPS_HOST" \
            "sleep 10 && ss -tlnp | grep 2083 || echo 'WARN: Port 2083 not listening yet - may need more time'"
          
          echo "✅ Xray VLESS REALITY config provisioned and applied"
          rm -f /tmp/deploy_key

      - name: Setup Marzban Admin from Secrets
        if: success()
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          ADMIN_USERNAME: ${{ secrets.MARZBAN_ADMIN_USERNAME }}
          ADMIN_PASSWORD: ${{ secrets.MARZBAN_ADMIN_PASSWORD }}
        run: |
          set -euo pipefail
          echo "🔐 Setting up SSH key..."
          echo "$SSH_PRIVATE_KEY" > /tmp/deploy_key
          chmod 600 /tmp/deploy_key

          echo "👥 Setting up Marzban admin from secrets..."
          scp -o StrictHostKeyChecking=no -i /tmp/deploy_key scripts/ci-setup-marzban-admin.sh "$VPS_USER@$VPS_HOST:/tmp/ci-setup-marzban-admin.sh"
          
          ssh -o StrictHostKeyChecking=no -i /tmp/deploy_key "$VPS_USER@$VPS_HOST" \
            "ADMIN_USERNAME='${ADMIN_USERNAME:-artur789298}'" \
            "ADMIN_PASSWORD='${ADMIN_PASSWORD:-WARpteN789298}'" \
            bash /tmp/ci-setup-marzban-admin.sh || echo "⚠️  Admin setup completed with warnings - check manually"
          
          echo "✅ Marzban admin setup step completed"
          rm -f /tmp/deploy_key

      - name: Final Dashboard Accessibility Check
        if: success()
        env:
          VPS_HOST: ${{ secrets.VPS_HOST }}
          VPS_USER: ${{ secrets.VPS_USER }}
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
          DOMAIN_NAME: ${{ secrets.DOMAIN_NAME }}
        run: |
          echo "🔐 Final accessibility check..."
          echo "$SSH_PRIVATE_KEY" > /tmp/final_key; chmod 600 /tmp/final_key
          CODE=$(ssh -o StrictHostKeyChecking=no -i /tmp/final_key "$VPS_USER@$VPS_HOST" "curl -sk -o /dev/null -w '%{http_code}' https://$DOMAIN_NAME/dashboard/" || echo "000")
          rm -f /tmp/final_key
          echo "🌐 Dashboard HTTP response: $CODE"
          case "$CODE" in
            200|302|401) echo "✅ SUCCESS";;
            000) echo "❌ CRITICAL: Dashboard completely inaccessible" && exit 1;;
            *) echo "⚠️  WARNING: Dashboard returned HTTP $CODE";;
          esac